<VirtualHost *:80>
    ServerName php.test
    DocumentRoot /app

    <Directory "/app">
        AllowOverride All
        Require all granted
    </Directory>

    <FilesMatch "\.php$">
        SetHandler "proxy:fcgi://php:9000"
    </FilesMatch>
</VirtualHost>
